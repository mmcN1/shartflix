import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF090909); // koyu arka plan
  static const Color primary = Color(0xFFE50914);   // kırmızı buton
  static const Color white = Colors.white;
  static const Color textGrey = Color.fromRGBO(255, 255, 255, 0.8);
  static const Color inputBorder = Colors.white;
  static const Color redColor = Color(0xFF3F0306);
}
